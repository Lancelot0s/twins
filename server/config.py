# PLC连接配置
PLC_CONFIG = {
    "ip": "127.0.0.1",  # 虚拟PLC的IP地址
    "rack": 0,           # 机架号
    "slot": 1            # 槽号
}

# WebSocket服务配置
WS_CONFIG = {
    "host": "127.0.0.1",  # 绑定本地地址
    "port": 8081           # WebSocket端口
}

# 数据读取间隔（秒）
READ_INTERVAL = 2
